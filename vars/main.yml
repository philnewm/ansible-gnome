---

dependencies:
  "RedHat":
    - python3-psutil
    - python3-lxml
    - glibc-langpack-en
    - curl  # required for gnome-shell-extension-installer
    - unzip  # required for gnome-shell-extension-installer
  "Debian":
    - python3-psutil
    - python3-lxml
    - curl  # required for gnome-shell-extension-installer
    - unzip  # required for gnome-shell-extension-installer

system_local_lang: "en_US.UTF-8"

...
